version: '3.9'

# PostgreSQL removido — banco hospedado no Supabase
# Redis não é utilizado pelo código atual — comentado para referência futura

services:
  # redis:
  #   image: redis:7-alpine
  #   container_name: myfinance_redis
  #   restart: unless-stopped
  #   ports:
  #     - '6379:6379'
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
